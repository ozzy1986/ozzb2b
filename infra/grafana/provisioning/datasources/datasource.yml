apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: ozzb2b-prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 15s

  - name: Loki
    type: loki
    uid: ozzb2b-loki
    access: proxy
    url: http://loki:3100
    editable: false
