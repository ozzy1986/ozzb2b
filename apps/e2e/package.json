{
  "name": "@ozzb2b/e2e",
  "version": "0.1.0",
  "private": true,
  "description": "End-to-end smoke tests (Playwright) for ozzb2b web.",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "install:browsers": "playwright install --with-deps chromium webkit"
  },
  "devDependencies": {
    "@playwright/test": "1.59.1",
    "@types/node": "22.9.0",
    "typescript": "5.6.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
